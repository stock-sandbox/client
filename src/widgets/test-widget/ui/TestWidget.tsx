'use client';

import { Box } from '@chakra-ui/react';

interface TestWidgetProps {
  // props
}

export const TestWidget = ({}: TestWidgetProps) => {
  return <Box>This is TestWidget Component</Box>;
};